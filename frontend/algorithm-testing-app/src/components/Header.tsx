import React from "react";

export const Header: React.FC = () => {
  return (
    <div className="header">
      <h1>Algorithm Testing Dashboard</h1>
      <p>Select an algorithm, configure parameters, and run tests</p>
    </div>
  );
};
